import MainContainer from "../components/common/MainContainer.tsx";
import { Typography } from "@mui/material";

const DemoNotes = () => {
    return (
        <MainContainer>
            <Typography>Demo Page with Notes</Typography>
        </MainContainer>
    );
};

export default DemoNotes;
