export type UserProfileLoginType = {
    userName: string;
    email: string;
    token: string;
};

export type UserProfileRegistrationType = {
    registered: boolean;
};

export type UserProfile = {
    userName: string;
    email: string;
};
